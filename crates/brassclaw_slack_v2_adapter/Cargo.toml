[package]
name = "brassclaw_slack_v2_adapter"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Slack v2 ProductAdapter for BrassClaw Reborn (#3857 tracer-bullet)"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/chtugha/brassclaw"
repository = "https://github.com/chtugha/brassclaw"
publish = false

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
brassclaw_product_adapters = { path = "../brassclaw_product_adapters", version = "0.1.0" }
brassclaw_turns = { path = "../brassclaw_turns", version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["time"] }

[dev-dependencies]
brassclaw_product_adapters = { path = "../brassclaw_product_adapters", version = "0.1.0", features = ["test-support", "host-auth-mint"] }
tokio = { version = "1", features = ["macros", "rt", "sync"] }
uuid = { version = "1", features = ["v4", "serde"] }
