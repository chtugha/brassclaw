[package]
name = "brassclaw_tui"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Modular Ratatui-based TUI for BrassClaw"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/chtugha/brassclaw"
repository = "https://github.com/chtugha/brassclaw"
publish = false

[features]
default = ["clipboard"]
clipboard = ["dep:arboard", "dep:image"]

[dependencies]
brassclaw_common = { path = "../brassclaw_common", version = "0.4.2" }
ratatui = { version = "0.29", features = ["crossterm"] }
tui-textarea = { version = "0.7", features = ["crossterm"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["sync", "macros", "rt", "time"] }
chrono = "0.4"
unicode-width = "0.2"
pulldown-cmark = { version = "0.12", default-features = false }
thiserror = "2"
tracing = "0.1"
arboard = { version = "3", optional = true }
image = { version = "0.25", default-features = false, features = ["png"], optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
tempfile = "3"
