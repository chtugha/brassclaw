// No app code imports from "react-dom" directly, but vendor bundles
// (e.g. react-router) may. Re-export from the combined react bundle
// so all consumers share one React instance.
export * from "react";
export { default } from "react";
