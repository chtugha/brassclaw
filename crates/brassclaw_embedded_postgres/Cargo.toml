[package]
name = "brassclaw_embedded_postgres"
version = "0.1.0"
edition = "2024"
publish = false
description = "Embedded PostgreSQL lifecycle management for brassclaw (download, initdb, pg_ctl, health check)."
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/chtugha/brassclaw"
repository = "https://github.com/chtugha/brassclaw"

[lints]
workspace = true

[features]
# Integration tests that require actually downloading and running PostgreSQL.
# Run with: cargo test -p brassclaw_embedded_postgres --features integration
integration = []

[dependencies]
# postgresql_embedded is kept only for the Settings / VersionReq types; the
# actual download is now done at compile time in build.rs so the runtime never
# makes outbound HTTP connections for Postgres binaries.
postgresql_embedded = { version = "0.21", features = ["tokio"] }
target-triple = "1"
sha2 = "0.10"
hex = "0.4"
thiserror = "2"
tokio = { version = "1", features = ["process", "fs", "net", "time", "rt", "macros"] }
tokio-postgres = "0.7"
tracing = "0.1"
libc = "0.2"
flate2 = "1"
tar = "0.4"

[build-dependencies]
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
brassclaw_pg = { path = "../brassclaw_pg" }
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }
tempfile = "3"
