[workspace]
members = [".", "crates/brassclaw_common", "crates/brassclaw_host_api", "crates/brassclaw_filesystem", "crates/brassclaw_memory", "crates/brassclaw_events", "crates/brassclaw_event_projections", "crates/brassclaw_event_streams", "crates/brassclaw_reborn_event_store", "crates/brassclaw_extensions", "crates/brassclaw_processes", "crates/brassclaw_dispatcher", "crates/brassclaw_scripts", "crates/brassclaw_process_sandbox", "crates/brassclaw_mcp", "crates/brassclaw_wasm", "crates/brassclaw_wasm_sandbox_core", "crates/brassclaw_wasm_limiter", "crates/brassclaw_capabilities", "crates/brassclaw_secrets", "crates/brassclaw_network", "crates/brassclaw_host_runtime", "crates/brassclaw_runtime_policy", "crates/brassclaw_authorization", "crates/brassclaw_run_state", "crates/brassclaw_approvals", "crates/brassclaw_resources", "crates/brassclaw_auth", "crates/brassclaw_trust", "crates/brassclaw_turns", "crates/brassclaw_agent_loop", "crates/brassclaw_threads", "crates/brassclaw_prompt_envelope", "crates/brassclaw_hooks", "crates/brassclaw_hooks_postgres", "crates/brassclaw_hooks_libsql", "crates/brassclaw_hooks_parity", "crates/brassclaw_loop_support", "crates/brassclaw_reborn", "crates/brassclaw_reborn_config", "crates/brassclaw_reborn_composition", "crates/brassclaw_reborn_identity", "crates/brassclaw_reborn_cli", "crates/brassclaw_reborn_traces", "crates/brassclaw_reborn_webui_ingress", "crates/brassclaw_conversations", "crates/brassclaw_product_adapters", "crates/brassclaw_product_workflow", "crates/brassclaw_product_workflow_storage", "crates/brassclaw_product_adapter_registry", "crates/brassclaw_wasm_product_adapters", "crates/brassclaw_telegram_v2_adapter", "crates/brassclaw_slack_v2_adapter", "crates/brassclaw_outbound", "crates/brassclaw_triggers", "crates/brassclaw_architecture", "crates/brassclaw_safety", "crates/brassclaw_skills", "crates/brassclaw_oauth", "crates/brassclaw_llm", "crates/brassclaw_embeddings", "crates/brassclaw_engine", "crates/brassclaw_gateway", "crates/brassclaw_gateway", "crates/brassclaw_first_party_extensions", "crates/brassclaw_webui_v2", "crates/brassclaw_webui_v2_static"]
exclude = [
    "channels-src/discord",
    "channels-src/feishu",
    "channels-src/telegram",
    "channels-src/slack",
    "channels-src/wechat",
    "channels-src/whatsapp",
    "crates/brassclaw_silk_decoder",
    "tools-src/composio",
    "tools-src/github",
    "tools-src/gmail",
    "tools-src/google-calendar",
    "tools-src/google-docs",
    "tools-src/google-drive",
    "tools-src/google-sheets",
    "tools-src/google-slides",
    "tools-src/slack",
    "tools-src/telegram",
    "fuzz",
    "crates/brassclaw_safety/fuzz",
]

# Maintainability guardrails against dead/speculative public API
# (the "Theme 1" anti-pattern from the maintainability audit: accessors,
# `_dyn` variants, and getters with zero callers that needlessly expand the
# stable surface). These are `warn`, not `deny`, and are *opted into*
# per-crate via `[lints] workspace = true` so they do not flood the build
# with pre-existing noise across the whole workspace. Currently scoped to the
# hook/reborn crates.
#
# - `unreachable_pub`: flags `pub` items that are not reachable outside their
#   crate — the classic "this should be `pub(crate)`" smell.
# - `dead_code`: warn on never-used items (this is the rustc default, declared
#   here so it survives any future `#![allow(dead_code)]` creep at the crate root).
[workspace.lints.rust]
unreachable_pub = "warn"
dead_code = "warn"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(disabled)'] }

[package]
name = "brassclaw"
version = "0.30.6"
edition = "2024"
rust-version = "1.92"
description = "Secure personal AI assistant that protects your data and expands its capabilities on the fly"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/chtugha/brassclaw"
repository = "https://github.com/chtugha/brassclaw"
default-run = "brassclaw"

[lints]
workspace = true

[package.metadata.wix]
upgrade-guid = "D0156E61-BA37-451E-8AB9-1A2ECCCFA48F"
path-guid = "F90B6EA6-87F7-499B-BB19-CF55DE1EB339"
license = false
eula = false

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tokio-util = { version = "0.7", features = ["compat"] }
futures = "0.3"
tokio-tungstenite = { version = "0.26", features = ["rustls-tls-native-roots"] }
eventsource-stream = "0.2"

# Agent Client Protocol (ACP) — standard communication with coding agents
agent-client-protocol = "0.10"

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls-native-roots", "stream"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Database - PostgreSQL (default, feature-gated)
deadpool-postgres = { version = "0.14", optional = true }
tokio-postgres = { version = "0.7", features = ["with-uuid-1", "with-chrono-0_4", "with-serde_json-1"], optional = true }
postgres-types = { version = "0.2", features = ["with-serde_json-1"], optional = true }
refinery = { version = "0.8", features = ["tokio-postgres"], optional = true }
tokio-postgres-rustls = { version = "0.13", optional = true }
rustls = { version = "0.23", optional = true, default-features = false }
rustls-native-certs = { version = "0.8", optional = true }
webpki-roots = { version = "0.26", optional = true }

# Database - libSQL/Turso (optional embedded database)
libsql = { version = "0.6", optional = true, default-features = false, features = ["core", "replication", "remote", "tls"] }

# Error handling
thiserror = "2"
anyhow = "1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Configuration
dotenvy = "0.15"
toml = "0.8"

# Core types
uuid = { version = "1", features = ["v4", "v5", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
iana-time-zone = "0.1"
rust_decimal = { version = "1", features = ["serde", "serde-with-str", "maths"] }
rust_decimal_macros = "1"

# Async traits
async-trait = "0.1"

# CLI
clap = { version = "4", features = ["derive", "env"] }

# Terminal
crossterm = "0.29"
rustyline = { version = "17", features = ["custom-bindings", "derive", "with-file-history"] }
termimad = "0.34"

# Channel integrations
axum = { version = "0.8", features = ["ws"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "cors", "set-header", "catch-panic"] }

# Cron scheduling for routines
cron = "0.13"

# JSON Schema validation (workspace document metadata).
# default-features disabled to avoid pulling a second `reqwest` major version
# for remote $ref resolution (we only validate against in-memory schemas).
jsonschema = { version = "0.45", default-features = false }

# Shared types
brassclaw_common = { path = "crates/brassclaw_common", version = "0.4.2" }
brassclaw_host_api = { path = "crates/brassclaw_host_api", version = "0.1.0" }
brassclaw_loop_support = { path = "crates/brassclaw_loop_support", version = "0.1.0" }
brassclaw_memory = { path = "crates/brassclaw_memory", version = "0.1.0" }
brassclaw_resources = { path = "crates/brassclaw_resources", version = "0.1.0" }
brassclaw_runtime_policy = { path = "crates/brassclaw_runtime_policy", version = "0.1.0" }
brassclaw_turns = { path = "crates/brassclaw_turns", version = "0.1.0" }

# V2 Reborn CLI
brassclaw_reborn_cli = { path = "crates/brassclaw_reborn_cli", version = "0.30.2" }

# Safety/sanitization
brassclaw_engine = { path = "crates/brassclaw_engine", version = "0.1.0" }
brassclaw_gateway = { path = "crates/brassclaw_gateway", version = "0.1.0" }
brassclaw_llm = { path = "crates/brassclaw_llm", version = "0.1.0" }
brassclaw_embeddings = { path = "crates/brassclaw_embeddings", version = "0.1.0" }
brassclaw_oauth = { path = "crates/brassclaw_oauth", version = "0.1.0" }
brassclaw_reborn_traces = { path = "crates/brassclaw_reborn_traces", version = "0.1.0" }
brassclaw_safety = { path = "crates/brassclaw_safety", version = "0.2.2" }
brassclaw_skills = { path = "crates/brassclaw_skills", version = "0.3.0" }
brassclaw_tui = { path = "crates/brassclaw_tui", optional = true, version = "0.1.0" }
regex = "1"
aho-corasick = "1"

# YAML parsing for SKILL.md frontmatter
serde_yml = "0.0.12"

# Filesystem paths
dirs = "6"
fs4 = "0.6"
glob = "0.3"

# Semantic versioning
semver = "1"

# Secrecy for sensitive values
secrecy = { version = "0.10", features = ["serde"] }

# URL parsing and encoding
url = "2"
urlencoding = "2"

# Open URLs in browser
open = "5"

# Vector embeddings for semantic search
# The postgres feature provides ToSql/FromSql for postgres-types (shared by tokio-postgres)
pgvector = { version = "0.4", features = ["postgres"], optional = true }

# WASM sandbox for untrusted tool execution
# Pinned to 44.0.2: minimum patch that contains the fix for
# RUSTSEC-2026-0149 (path_open(TRUNCATE) bypassing FilePerms::WRITE
# host restriction). 43.x is end-of-life for this advisory.
wasmtime = { version = "44.0.2", features = ["component-model"] }
wasmtime-wasi = "44.0.2"  # WASI support for component model
wasmparser = "0.246.2"  # WASM binary parsing for validation

# Cryptography for secrets management
aes = "0.8"
aes-gcm = "0.10"
hkdf = "0.12"
hmac = "0.12"
md-5 = "0.10"
sha2 = "0.10"
blake3 = "1"
rand = "0.8"
subtle = "2"  # Constant-time comparisons for token validation

# Multi-provider LLM support
rig-core = { version = "0.30", default-features = false, features = ["reqwest-rustls"] }

# AWS Bedrock: deps now live in `brassclaw_llm` (LLM provider) and
# `brassclaw_embeddings` (Titan embeddings); the top-level `bedrock` feature
# (below) re-exposes both via crate-feature forwarding.

# Docker sandbox
bollard = "0.18"

# Archive extraction for WASM extension bundles
flate2 = "1"
tar = "0.4"

# Document text extraction
pdf-extract = "0.7"
zip = { version = "2", default-features = false, features = ["deflate"] }

# HTTP proxy for sandboxed network access
hyper = { version = "1.5", features = ["server", "http1", "http2"] }
hyper-util = { version = "0.1", features = ["server", "tokio", "http1", "http2"] }
http-body-util = "0.1"
bytes = "1"
base64 = "0.22.1"
cookie = "0.18"
jsonwebtoken = "9"
mime_guess = "2.0.5"
clap_complete = "4.5.0"
lru = "0.16.3"

# HTML to Markdown conversion (feature gated)
html-to-markdown-rs = { version = "2.3", optional = true }
readabilityrs = { version = "0.1.2", optional = true }
ed25519-dalek = { version = "2.2.0", features = ["std"] }
bs58 = "0.5"
hex = "0.4.3"

# Reborn capability system
brassclaw_capabilities = { path = "crates/brassclaw_capabilities", version = "0.1.0" }
brassclaw_extensions = { path = "crates/brassclaw_extensions", version = "0.1.0" }
brassclaw_authorization = { path = "crates/brassclaw_authorization", version = "0.1.0" }
brassclaw_trust = { path = "crates/brassclaw_trust", version = "0.1.0" }

# OpenClaw import (feature gated)
json5 = { version = "0.4", optional = true }

# macOS keychain
[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3"

# PTY allocation for Claude CLI stdout buffering fix (Unix only)
[target.'cfg(unix)'.dependencies]
pty-process = { version = "0.5", features = ["async"] }

# Linux secret-service (GNOME Keyring, KWallet)
[target.'cfg(target_os = "linux")'.dependencies]
secret-service = { version = "4", features = ["rt-tokio-crypto-rust"] }
zbus = "4"

[build-dependencies]
serde_json = "1"

[dev-dependencies]
tokio-test = "0.4"
tracing-test = "0.2"
testcontainers-modules = { version = "0.11", features = ["postgres"] }
pretty_assertions = "1"
tempfile = "3"
insta = { version = "1.46.3", features = ["yaml"] }
# Pull the LLM crate's `testing` feature in only for tests so `StubLlm` and
# fault-injection helpers don't ship in release binaries.
brassclaw_llm = { path = "crates/brassclaw_llm", version = "0.1.0", features = ["testing"] }
brassclaw_authorization = { path = "crates/brassclaw_authorization", version = "0.1.0" }
brassclaw_extensions = { path = "crates/brassclaw_extensions", version = "0.1.0" }
brassclaw_filesystem = { path = "crates/brassclaw_filesystem", version = "0.1.0" }
brassclaw_host_runtime = { path = "crates/brassclaw_host_runtime", version = "0.1.0" }
brassclaw_network = { path = "crates/brassclaw_network", version = "0.1.0" }
brassclaw_processes = { path = "crates/brassclaw_processes", version = "0.1.0" }
brassclaw_resources = { path = "crates/brassclaw_resources", version = "0.1.0" }
brassclaw_secrets = { path = "crates/brassclaw_secrets", version = "0.1.0" }
brassclaw_product_adapters = { path = "crates/brassclaw_product_adapters", version = "0.1.0", features = ["test-support"] }
brassclaw_product_workflow = { path = "crates/brassclaw_product_workflow", version = "0.1.0", features = ["test-support"] }
brassclaw_reborn = { path = "crates/brassclaw_reborn", version = "0.1.0" }
brassclaw_reborn_composition = { path = "crates/brassclaw_reborn_composition", version = "0.1.0" }
brassclaw_threads = { path = "crates/brassclaw_threads", version = "0.1.0" }
brassclaw_triggers = { path = "crates/brassclaw_triggers", version = "0.1.0" }
brassclaw_trust = { path = "crates/brassclaw_trust", version = "0.1.0" }
brassclaw_wasm = { path = "crates/brassclaw_wasm", version = "0.1.0" }
# Same idea for embeddings: `MockEmbeddings` is only reachable in dev builds.
brassclaw_embeddings = { path = "crates/brassclaw_embeddings", version = "0.1.0", features = ["testing"] }

[features]
default = ["postgres", "libsql", "html-to-markdown", "tui"]
postgres = [
    "dep:deadpool-postgres",
    "dep:tokio-postgres",
    "dep:tokio-postgres-rustls",
    "dep:rustls",
    "dep:rustls-native-certs",
    "dep:webpki-roots",
    "dep:postgres-types",
    "dep:refinery",
    "dep:pgvector",
    "rust_decimal/db-tokio-postgres",
]
libsql = ["dep:libsql"]
# Opt-in feature for especially heavy integration-test targets that run in a
# dedicated CI job instead of the default Rust test matrix.
integration = []
# Replay-gate snapshot tests promoted out of `--ignored` into a dedicated CI
# job. Enabled by `.github/workflows/replay-gate.yml`; implies `libsql` so the
# test rig can spin up its embedded database.
replay = ["libsql"]
html-to-markdown = ["dep:html-to-markdown-rs", "dep:readabilityrs"]
bedrock = ["brassclaw_llm/bedrock", "brassclaw_embeddings/bedrock"]
tui = ["dep:brassclaw_tui"]
import = ["dep:json5", "libsql"]
# Conditional gate for memory-substrate regression tests that anticipate
# specific guards: `ensure_path_matches_context`/`ensure_scope_matches_context`
# fail-closed checks, deterministic tie-breaking by path ascending,
# `.system/engine/orchestrator/*` protected-path registration, and
# min_score-after-normalization. #3180 as merged delivers the substrate
# skeleton but NOT these specific behaviors — the followup PR(s) that
# implement them must enable this feature in their merge commit so the
# gated tests fire in CI. Mirrors the same-named feature on
# `brassclaw_memory`.
#
# Empirical note (2026-05-12): enabling `pr3180-ready` on the current
# `reborn-integration` HEAD surfaces 7 gated tests as failing, because
# the anticipated APIs do not exist in the merged substrate yet. Do not
# enable in CI until the followup landings.
pr3180-ready = []
# Conditional gate for caller-tier (tool-dispatcher → substrate) tests.
# Distinct from `pr3180-ready` because the substrate-level guards and
# the tool-routing migration are separate landings: PR 7 routes
# `memory_write` through `brassclaw_memory`'s `PromptWriteSafetyPolicy`,
# so until PR 7 lands, tool-layer rejection tests run against the
# legacy host-workspace path which doesn't consult the substrate.
# PR 7 must enable this feature in its merge commit.
pr7-ready = []

[profile.release]
strip = true          # Remove debug symbols from release binaries

# The profile that 'cargo dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"

# Config for 'dist'
[workspace.metadata.dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.31.0"
# Ignore out-of-date generated CI so custom release.yml jobs are allowed
allow-dirty = ["ci"]
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "npm", "msi"]
# Publish jobs to run in CI
publish-jobs = []
# Target platforms to build apps for (Rust target-triple syntax)
targets = [
    "aarch64-apple-darwin",
    "aarch64-unknown-linux-gnu",
    "aarch64-unknown-linux-musl",
    "x86_64-apple-darwin",
    "x86_64-unknown-linux-gnu",
    "x86_64-unknown-linux-musl",
    "x86_64-pc-windows-msvc",
]
# The archive format to use for windows builds (defaults .zip)
windows-archive = ".tar.gz"
# The archive format to use for non-windows builds (defaults .tar.xz)
unix-archive = ".tar.gz"
# Which actions to run on pull requests
pr-run-mode = "skip"
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = true
# Cache intermediate build artifacts to speed up the release pipelines
cache-builds = true

[workspace.metadata.dist.github-custom-runners]
aarch64-unknown-linux-gnu = "ubuntu-24.04-arm"
aarch64-unknown-linux-musl = "ubuntu-24.04-arm"
x86_64-unknown-linux-gnu = "ubuntu-22.04"
x86_64-unknown-linux-musl = "ubuntu-22.04"
x86_64-pc-windows-msvc = "windows-2022"
x86_64-apple-darwin = "macos-15-intel"
aarch64-apple-darwin = "macos-14"
